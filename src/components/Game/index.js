import Board from '../Board';

const Game = (props) => {
    return (
        <div className="game">
            <h1>Tic-Tac-Toe</h1>
            <Board />
        </div>
    );
};

export default Game;
